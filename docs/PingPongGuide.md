# PING/PONG 연결 상태 확인 가이드

> 클라이언트 연결 상태를 주기적으로 확인하는 하트비트(Heartbeat) 메커니즘

---

## 왜 PING/PONG이 필요한가?

### 문제 상황

```
┌─────────────┐                              ┌─────────────┐
│  클라이언트  │ ──── TCP 연결 ────────────▶ │    서버     │
└─────────────┘                              └─────────────┘
       │
       │ (갑자기 전원 OFF, 네트워크 끊김)
       │
       ✖ 연결이 끊어졌지만...

       서버는 모른다! (TCP는 즉시 감지 못함)
       └── 좀비 세션이 계속 남아있음
       └── 메모리 낭비
       └── 다른 플레이어에게 유령으로 보임
```

### 해결책: PING/PONG

```
서버: "야, 살아있어?" (PING)
          │
          ▼
클라이언트: "응, 살아있어!" (PONG)
          │
          └── 3초 안에 응답 없으면 → 연결 끊김 처리
```

---

## 전체 동작 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      PING/PONG 타임라인                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  시간 →                                                                  │
│                                                                          │
│  0초        10초       20초       30초       40초                        │
│  │          │          │          │          │                          │
│  │          │          │          │          │                          │
│  └── PING ──┴── PING ──┴── PING ──┴── PING ──┴──▶                       │
│      │          │          │          │                                  │
│      ▼          ▼          ▼          ▼                                  │
│    PONG ✅    PONG ✅    PONG ✅    (없음) ❌                             │
│                                        │                                  │
│                                        ▼                                  │
│                                   3초 대기...                            │
│                                        │                                  │
│                                        ▼                                  │
│                                   연결 끊김!                             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 설정 값

**파일:** `GameServer/Room.h:39-40`

```cpp
static const uint64 _pingInterval = 10000;  // 10초 (10,000ms)
static const uint64 _pongTimeout = 3000;    // 3초 (3,000ms)
```

| 설정 | 값 | 의미 |
|------|-----|------|
| `_pingInterval` | 10초 | PING 전송 간격 |
| `_pongTimeout` | 3초 | PONG 응답 대기 시간 |

---

## 관련 데이터 구조

### GameSession 변수

**파일:** `GameServer/GameSession.h:24-25`

```cpp
class GameSession : public PacketSession
{
public:
    atomic<uint64> _lastPingTime = 0;  // 마지막으로 PING 보낸 시간
    atomic<uint64> _pongTime = 0;      // 클라이언트가 PONG 응답한 시간
};
```

### 패킷 정의

```protobuf
// Protocol.proto
message S2C_PING {
    uint64 timestamp = 1;  // 서버 시간 (옵션)
}

message C2S_PONG {
    uint64 timestamp = 1;  // 서버가 보낸 시간 그대로 반환 (옵션)
}
```

---

## 코드 상세 분석

### 1. PING 전송 (서버 → 클라이언트)

**파일:** `GameServer/Room.cpp:36-85`

```cpp
void Room::SendPing()
{
    uint64 now = ::GetTickCount64();
    vector<int32> timeoutPlayers;

    // 모든 플레이어 순회
    for (auto& [id, player] : _players)
    {
        if (auto session = player->session.lock())
        {
            uint64 lastPing = session->_lastPingTime.load();
            uint64 pongTime = session->_pongTime.load();

            // ① 이전 PING에 대한 PONG 체크 (첫 PING 제외)
            if (lastPing > 0)
            {
                // PONG 안 왔거나, 3초 초과
                if (pongTime == 0 || (pongTime - lastPing) > _pongTimeout)
                {
                    timeoutPlayers.push_back(id);
                    continue;
                }
            }

            // ② 새 PING 준비
            session->_lastPingTime.store(now);
            session->_pongTime.store(0);  // 리셋
        }
    }

    // ③ 타임아웃 처리
    for (int32 id : timeoutPlayers)
    {
        if (auto player = _players[id])
        {
            if (auto session = player->session.lock())
            {
                session->Disconnect(L"Pong timeout");
            }
        }
    }

    // ④ PING 패킷 전송 (모든 플레이어에게)
    Protocol::S2C_PING ping;
    ping.set_timestamp(now);
    SendBufferRef sendBuffer = ServerPacketHandler::MakeSendBuffer(ping);
    Broadcast(sendBuffer);

    // ⑤ 다음 PING 예약 (10초 후)
    DoTimer(_pingInterval, &Room::SendPing);
}
```

### 동작 순서도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Room::SendPing() 실행                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  1단계: 이전 PING 응답 체크                                        │  │
│  │                                                                    │  │
│  │  for (각 플레이어)                                                 │  │
│  │  {                                                                 │  │
│  │      if (lastPing > 0)  // 첫 PING이 아니면                       │  │
│  │      {                                                             │  │
│  │          if (pongTime == 0)           → 타임아웃! (PONG 안 옴)    │  │
│  │          if (pongTime - lastPing > 3초) → 타임아웃! (너무 늦음)   │  │
│  │      }                                                             │  │
│  │  }                                                                 │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                              │                                           │
│                              ▼                                           │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  2단계: 타임아웃 플레이어 연결 끊기                                 │  │
│  │                                                                    │  │
│  │  for (타임아웃 플레이어들)                                         │  │
│  │  {                                                                 │  │
│  │      session->Disconnect(L"Pong timeout");                        │  │
│  │  }                                                                 │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                              │                                           │
│                              ▼                                           │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  3단계: 새 PING 전송                                               │  │
│  │                                                                    │  │
│  │  _lastPingTime = now     // PING 보낸 시간 기록                   │  │
│  │  _pongTime = 0           // PONG 시간 리셋                        │  │
│  │  Broadcast(S2C_PING)     // 모든 플레이어에게 PING                │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                              │                                           │
│                              ▼                                           │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │  4단계: 다음 PING 예약                                             │  │
│  │                                                                    │  │
│  │  DoTimer(10000, &Room::SendPing)  // 10초 후 다시 실행            │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 2. PONG 수신 (클라이언트 → 서버)

**파일:** `GameServer/ServerPacketHandler.cpp:37-42`

```cpp
bool Handle_C2S_PONG(GameSessionRef& session, Protocol::C2S_PONG& pkt)
{
    uint64 expected = 0;
    session->_pongTime.compare_exchange_strong(expected, ::GetTickCount64());
    return true;
}
```

### compare_exchange_strong 설명

```cpp
session->_pongTime.compare_exchange_strong(expected, ::GetTickCount64());
```

```
의미: "_pongTime이 0이면 현재 시간으로 변경해라"

┌─────────────────────────────────────────────────────────────────────────┐
│  compare_exchange_strong(expected, new_value)                           │
│                                                                          │
│  if (_pongTime == expected)  // _pongTime == 0 이면                     │
│  {                                                                       │
│      _pongTime = new_value;  // 현재 시간으로 설정                       │
│      return true;                                                        │
│  }                                                                       │
│  else                                                                    │
│  {                                                                       │
│      expected = _pongTime;   // 이미 값이 있으면 무시                    │
│      return false;                                                       │
│  }                                                                       │
└─────────────────────────────────────────────────────────────────────────┘
```

**왜 이렇게 하는가?**
- 중복 PONG 방지 (클라이언트가 여러 번 보내도 첫 번째만 인정)
- Lock 없이 스레드 안전

---

### 3. PING 시작 (서버 초기화)

**파일:** `GameServer/Room.cpp:32-33`

```cpp
void Room::Init(int mapId)
{
    // ... 맵 초기화 ...

    DoTimer(50, &Room::Update);
    DoTimer(_pingInterval, &Room::SendPing);  // ← 첫 PING 예약 (10초 후)
}
```

---

## 시퀀스 다이어그램

```
       서버 (Room)                    클라이언트 A              클라이언트 B
           │                              │                         │
           │                              │                         │
     ┌─────┴─────┐                        │                         │
     │ Init()    │                        │                         │
     │ DoTimer() │                        │                         │
     └─────┬─────┘                        │                         │
           │                              │                         │
           │ ─────── 10초 경과 ───────    │                         │
           │                              │                         │
     ┌─────┴─────┐                        │                         │
     │SendPing() │                        │                         │
     └─────┬─────┘                        │                         │
           │                              │                         │
           │ ── S2C_PING ───────────────▶ │                         │
           │ ── S2C_PING ─────────────────────────────────────────▶ │
           │                              │                         │
           │      _lastPingTime = now     │                         │
           │      _pongTime = 0           │                         │
           │                              │                         │
           │ ◀─ C2S_PONG ──────────────── │ (1초 후 응답)           │
           │                              │                         │
           │      _pongTime = now ✅      │                         │
           │                              │                         │
           │                              │        (응답 없음) ❌   │
           │                              │                         │
           │ ─────── 10초 경과 ───────    │                         │
           │                              │                         │
     ┌─────┴─────┐                        │                         │
     │SendPing() │                        │                         │
     │ 체크:     │                        │                         │
     │ A: OK ✅  │                        │                         │
     │ B: 타임아웃│                        │                         │
     └─────┬─────┘                        │                         │
           │                              │                         │
           │ ── Disconnect ─────────────────────────────────────▶  ✖
           │                              │
           │ ── S2C_PING ───────────────▶ │
           │                              │
```

---

## 상태 전이도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      플레이어 연결 상태                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│     ┌─────────────┐                                                     │
│     │   연결됨     │                                                     │
│     │ (정상 상태)  │◀──────────────────────────────────┐                │
│     └──────┬──────┘                                    │                │
│            │                                           │                │
│            │ S2C_PING 수신                             │                │
│            ▼                                           │                │
│     ┌─────────────┐                                    │                │
│     │  PONG 대기   │                                    │                │
│     │ (3초 타이머) │                                    │                │
│     └──────┬──────┘                                    │                │
│            │                                           │                │
│       ┌────┴────┐                                      │                │
│       │         │                                      │                │
│       ▼         ▼                                      │                │
│  ┌─────────┐  ┌─────────┐                             │                │
│  │C2S_PONG │  │ 타임아웃 │                             │                │
│  │  수신   │  │ (3초)   │                             │                │
│  └────┬────┘  └────┬────┘                             │                │
│       │            │                                   │                │
│       │            ▼                                   │                │
│       │     ┌─────────────┐                           │                │
│       │     │  연결 끊김   │                           │                │
│       │     │ Disconnect() │                           │                │
│       │     └─────────────┘                           │                │
│       │                                                │                │
│       └────────────────────────────────────────────────┘                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 코드 위치 정리

| 구성 요소 | 파일 | 라인 | 설명 |
|----------|------|------|------|
| 설정 값 | `Room.h` | 39-40 | `_pingInterval`, `_pongTimeout` |
| 세션 변수 | `GameSession.h` | 24-25 | `_lastPingTime`, `_pongTime` |
| PING 시작 | `Room.cpp` | 33 | `DoTimer(_pingInterval, &Room::SendPing)` |
| PING 전송 | `Room.cpp` | 36-85 | `SendPing()` |
| PONG 처리 | `ServerPacketHandler.cpp` | 37-42 | `Handle_C2S_PONG()` |

---

## 튜닝 가이드

| 환경 | _pingInterval | _pongTimeout | 설명 |
|------|---------------|--------------|------|
| 개발 | 30초 | 10초 | 디버깅 편의 (느슨하게) |
| 테스트 | 10초 | 3초 | 현재 설정 (표준) |
| 운영 (모바일) | 15초 | 5초 | 네트워크 불안정 고려 |
| 운영 (PC) | 10초 | 3초 | 빠른 감지 |
| 실시간 대전 | 5초 | 2초 | 매우 빠른 감지 필요 |

**주의:** 너무 짧으면 일시적 네트워크 지연에도 끊김 발생

---

## 요약

```
┌────────────────────────────────────────────────────────────────────────┐
│                         PING/PONG 요약                                  │
├────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  목적: 클라이언트 연결 상태 확인 (좀비 세션 방지)                        │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 서버                           클라이언트                        │   │
│  │   │                               │                              │   │
│  │   │ ─── S2C_PING (10초마다) ────▶ │                              │   │
│  │   │                               │                              │   │
│  │   │ ◀── C2S_PONG (3초 이내) ───── │                              │   │
│  │   │                               │                              │   │
│  │   │        응답 없으면?           │                              │   │
│  │   │              ↓                │                              │   │
│  │   │      Disconnect() 호출        │                              │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  핵심 변수:                                                             │
│  • _lastPingTime: PING 보낸 시간                                       │
│  • _pongTime: PONG 받은 시간                                           │
│  • _pingInterval: 10초 (PING 주기)                                     │
│  • _pongTimeout: 3초 (응답 대기 시간)                                  │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘
```

---

> 문서 작성일: 2026-01-08
> 대상 프로젝트: NamoServer
